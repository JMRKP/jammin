<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.hdm_stuttgart.jammin">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="de.hdm_stuttgart.jammin.core.StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="de.hdm_stuttgart.jammin.repertoire.RepertoireActivity" />
        <activity android:name="de.hdm_stuttgart.jammin.client.JoinSessionActivity" />
        <activity android:name="de.hdm_stuttgart.jammin.server.StartSessionActivity" />
        <activity android:name="de.hdm_stuttgart.jammin.client.SessionRepertoireActivity"></activity>
    </application>

</manifest>